import 'package:flutter/cupertino.dart';

class ListsContainer extends StatelessWidget {
  const ListsContainer({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
