import 'package:flutter/cupertino.dart';

class MoreContainer extends StatelessWidget {
  const MoreContainer({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
