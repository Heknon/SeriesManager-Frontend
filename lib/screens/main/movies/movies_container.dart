import 'package:flutter/cupertino.dart';

class MoviesContainer extends StatelessWidget {
  const MoviesContainer({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
