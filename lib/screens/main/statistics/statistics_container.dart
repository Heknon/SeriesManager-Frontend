import 'package:flutter/cupertino.dart';

class StatisticsContainer extends StatelessWidget {
  const StatisticsContainer({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
